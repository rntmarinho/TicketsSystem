# Segredos e configuração local — nunca versionar
.env
backend/.env
frontend/.env
*.env.local

# Python
__pycache__/
*.pyc

# Node
node_modules/

# Uploads de usuário (não são código-fonte)
backend/public/anexos/*
!backend/public/anexos/.gitkeep

# Compose local de teste (portas/nomes isolados da instância em produção)
docker-compose.test.yml
