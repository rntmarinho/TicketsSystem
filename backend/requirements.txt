# Framework Web e Extensões
Flask>=3.0.0
Flask-Cors>=4.0.0
Flask-JWT-Extended>=4.6.0
Flask-Limiter>=3.7.0

# Criptografia e Segurança
bcrypt>=4.1.2
cryptography>=42.0.0

# Driver de Banco de Dados (PostgreSQL)
psycopg2-binary>=2.9.9

# ORM e migrations do módulo de Gestão de Projetos (APPCNS) — só essas
# tabelas novas usam SQLAlchemy/Alembic; as tabelas legadas (tbl_*) continuam
# em SQL cru via psycopg2, sem migração de ORM (ver plano de fusão).
SQLAlchemy>=2.0.30
alembic>=1.13.2
python-dateutil>=2.9.0

# Gerenciamento de Variáveis de Ambiente
python-dotenv>=1.0.1


imap_tools>=0.7.0

# Sanitização de HTML (módulo de Anotações — conteúdo rico exibido pra outros usuários)
bleach>=6.1.0
